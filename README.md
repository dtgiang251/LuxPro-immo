# LuxPro-immo

React + Tailwind landing page for LuxPro immo.

## Design note
The current layout mirrors the written requirements, but the referenced Figma image was not available in the repository or conversation transcript. Please provide the source design (image export or shared link) so spacing, typography, and asset usage can be aligned precisely with the intended UI.

## Getting started

```bash
npm install
npm run dev
```

If your environment blocks access to the public npm registry, configure your network/proxy before running `npm install`.
